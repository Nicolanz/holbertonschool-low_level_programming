#include "holberton.h"
/**
 * _strlen - Prints the size of a string.
 * @s: parameter to get the size.
 *
 * Return: Always 0 (Success)
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != 0; i++)
	{
	}
	return (i);
}
